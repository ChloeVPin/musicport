{
  "name": "musicport",
  "private": true,
  "version": "0.1.0",
  "description": "Move music onto your iPhone locally - desktop app (Tauri 2 + vanilla TS/Vite).",
  "workspaces": [
    "ui"
  ],
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "npm run build --workspace ui",
    "preview": "npm run preview --workspace ui",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}